# Env vars the legacy server already depends on but never documented
# (server/config/local.env.sample.js doesn't list these).
PORT=4000
SOCRATA_USERNAME=
SOCRATA_PASSWORD=
SOCRATA_APP_TOKEN_MTC=
VMT_DATA_KEY=

# No DATABASE_URL by default — the new API is stateless (Socrata proxy only).
# Add one only if the feedback task (plan Task 5) decides to bring feedback in-house.
