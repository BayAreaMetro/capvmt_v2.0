# Server-side environment variables (see web/lib/env.ts). Read directly
# by this app's Route Handlers (web/app/api/*) - no separate API service.
SOCRATA_USERNAME=
SOCRATA_PASSWORD=
SOCRATA_APP_TOKEN_MTC=
VMT_DATA_KEY=

# No DATABASE_URL — the app stays stateless.
